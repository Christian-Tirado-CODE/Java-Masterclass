package com.company;

public interface CanFly {
    public void fly();

    // In this case, this interface adds flexibility to our design.
    // Bats can fly(glide) but they are not birds.
    //
}
